#include<bits/stdc++.h>
#define rep(n) for(int i=0;i<n;i++)
using namespace std;
int main()
{
    int n,sum=0;cin>>n;int arr[n];
    rep(n) cin>>arr[i];
    sort(arr,arr+n);
    rep(n/2+1) sum+=arr[i]/2+1;
    cout<<sum;
}
